# metrics/

**Purpose**: Tag metrics calculation and management

**Main functions**:
- `calculator.py`: Core metric calculation logic for skills, technologies, and tags

**Dependent files**:
- `db/models.py`: Database operations
- DB config (`metrics` namespace): Metric formula configuration
