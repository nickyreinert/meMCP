# connectors/.env — copy this file to .env and fill in your values
# This file is safe to commit. The actual .env is gitignored.

# Required when chat.host=groq in config.yaml
GROQ_API_KEY=

# Shared secret between bot adapters and this proxy.
# Bot adapters must send: X-Proxy-Secret: <value>
# Also used as the encryption key for tokens stored in proxy.db.
# If unset: no auth check, tokens stored plaintext (development mode only).
PROXY_SECRET=

# Optional: override values from config.yaml chat: section
# MEMCP_URL=http://localhost:8000
# PROXY_DB_PATH=data/proxy.db
