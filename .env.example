# Admin Backend credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
ADMIN_SECRET_KEY=changeme-generate-a-real-secret

# Optional: JWT expiration in minutes (default: 60)
# ADMIN_TOKEN_EXPIRE_MINUTES=60

# Optional: restrict CORS origins (comma-separated, default: *)
# ADMIN_CORS_ORIGINS=https://admin.example.com
